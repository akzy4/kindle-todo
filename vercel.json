{
  "version": 2,
  "buildCommand": "if [ -f frontend/package.json ]; then npm --prefix frontend run build -- --outDir ../dist; else npm run build -- --outDir dist; fi",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/todos/:id", "destination": "/api/todos?id=:id" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
